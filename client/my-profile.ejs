<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white">
    <div class="container mx-auto p-8 max-w-md bg-gray-800 rounded-md shadow-md mt-16">
        <h1 class="text-3xl font-bold text-blue-400 mb-4">Welcome, <%= user.username %></h1>

        <ul class="mb-8">
            <li><a href="/community/my_communities" class="text-blue-400 hover:underline">My Communities</a></li>
            <li><a href="/profile/my_connections" class="text-blue-400 hover:underline">My Connections</a></li>
        </ul>

        <a href="/profile/edit" class="bg-blue-500 text-white px-2 py-2 mb-2 rounded hover:bg-gray-700">Edit Profile</a>

        <div class="my-4">
            <p class="text-gray-400">Username: <%= user.username %></p>
            <p class="text-gray-400">Email: <%= user.email %></p>
            <p class="text-gray-400">Phone: <%= user.phone ? user.phone : 'Not provided' %></p>
            <p class="text-gray-400">Birthday: <%= user.dob ? new Date(user.dob).toLocaleDateString() : 'Not provided' %></p>
        </div>

        <a href="/home" class="bg-blue-500 text-white px-2 py-2 mt-2 rounded hover:bg-blue-600">Back</a>
    </div>
</body>
</html>
