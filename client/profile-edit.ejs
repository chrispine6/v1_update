<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
    <!-- Include the compiled Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white">
    <div class="container mx-auto p-8 max-w-md bg-gray-800 rounded-md shadow-md mt-16">
        <h1 class="text-3xl font-bold text-blue-400 mb-4">Edit Profile</h1>
        <form action="/profile/edit" method="POST">
            <div class="mb-4">
                <label for="name" class="block text-gray-400">Name</label>
                <input type="text" id="name" name="name" value="<%= user.name %>" class="w-full text-black px-2 py-2 rounded border border-gray-600" required>
            </div>
            <div class="mb-4">
                <label for="email" class="block text-gray-400">Email</label>
                <input type="email" id="email" name="email" value="<%= user.email %>" class="w-full text-black px-2 py-2 rounded border border-gray-600" required>
            </div>
            <div class="mb-4">
                <label for="phone" class="block text-gray-400">Phone</label>
                <input type="text" id="phone" name="phone" value="<%= user.phone %>" class="w-full text-black px-2 py-2 rounded border border-gray-600">
            </div>
            <button type="submit" class="bg-blue-400 text-white px-4 py-2 rounded hover:bg-blue-600">Save Changes</button>
        </form>
        <a href="/profile" class="block mt-4 text-blue-400 hover:underline">Cancel</a>
    </div>
</body>
</html>
