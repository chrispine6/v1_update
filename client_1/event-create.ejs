<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Event</title>
    <!-- Include the compiled Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white">
    <div class="container mx-auto p-8 max-w-md bg-gray-800 rounded-md shadow-md mt-16">
        <h2 class="text-3xl font-bold text-blue-300 mb-8">Create Event</h2>
        <form action="/event/create" method="POST" class="space-y-4 mb-3">
            <div>
                <label for="eventCommunity" class="block text-gray-400">Event Community:</label>
                <select id="eventCommunity" name="eventCommunity" required class="w-full px-4 py-2 border border-gray-600 rounded-md focus:outline-none focus:border-blue-500 bg-gray-700 text-white">
                    <% communities.forEach(community => { %>
                        <option value="<%= community._id %>"><%= community.name %></option>
                    <% }) %>
                </select>
            </div>
            <div>
                <label for="eventDate" class="block text-gray-400">Event Date:</label>
                <input type="date" id="eventDate" name="eventDate" required class="w-full px-4 py-2 border border-gray-600 rounded-md focus:outline-none focus:border-blue-500 bg-gray-700 text-white">
            </div>
            <div>
                <label for="eventStartTime" class="block text-gray-400">Event Start Time:</label>
                <input type="time" id="eventStartTime" name="eventStartTime" required class="w-full px-4 py-2 border border-gray-600 rounded-md focus:outline-none focus:border-blue-500 bg-gray-700 text-white">
            </div>
            <div>
                <label for="eventEndTime" class="block text-gray-400">Event End Time:</label>
                <input type="time" id="eventEndTime" name="eventEndTime" required class="w-full px-4 py-2 border border-gray-600 rounded-md focus:outline-none focus:border-blue-500 bg-gray-700 text-white">
            </div>
            <div>
                <label for="eventTitle" class="block text-gray-400">Event Title:</label>
                <input type="text" id="eventTitle" name="eventTitle" required class="w-full px-4 py-2 border border-gray-600 rounded-md focus:outline-none focus:border-blue-500 bg-gray-700 text-white">
            </div>
            <div>
                <label for="eventContent" class="block text-gray-400">Event Content:</label>
                <textarea id="eventContent" name="eventContent" required class="w-full px-4 py-2 border border-gray-600 rounded-md focus:outline-none focus:border-blue-500 bg-gray-700 text-white"></textarea>
            </div>
            <button type="submit" class="w-full bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-600">Create</button>
        </form>

        <a href="/home" class="bg-blue-500 text-white px-2 py-2 rounded hover:bg-blue-600">Back</a>

    </div>
</body>
</html>
